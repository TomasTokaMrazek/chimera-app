export interface OauthTokens {
    accessToken: string;
    refreshToken: string;
}

export interface SocketToken {
    socketToken: string;
}

export interface AccountIds {
    twitch: string;
    streamLabs: string;
}
